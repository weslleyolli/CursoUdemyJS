// alert, confirm, prompt
window.alert('With our message')
const confirmed = window.confirm("Want continue in page?");
console.log (confirmed);
let num = parseInt(window.prompt("enter a number:"));
let num2 = parseInt(window.prompt("enter another number:"));
let result = (num + num2);
console.log(result);