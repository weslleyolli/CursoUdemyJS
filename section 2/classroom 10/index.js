/**
 * + sum/ concatenation
 * - subtration
 * * multiplication
 * / division
 * % rest
 * ** square root
 */

let cont = 1;
cont++ ;// increment cont = 2
cont--; // decrement cont = 1
console.log(cont);

const step = 2;
cont = cont + step;
console.log(cont);

cont += step;
console.log(cont)

const num = parseInt("5") //transforming the string in number
console.log(typeof num);